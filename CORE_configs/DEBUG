Debugging guide:

If a simulation isn't working (or specifically one node):

1) make sure that ION started
	go to the node and see if ion.log is present
	if so, go to 2)
	if not, restart the sim or start up ion manually

2) make sure that nodeX.ionrc shows the routes or the pattern you expect
	If the sim has mobility, the ionrc is generated by 
	a script called rcgen.sh.  check this for issues.

3) 
